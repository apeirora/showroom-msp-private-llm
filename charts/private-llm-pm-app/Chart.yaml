apiVersion: v2
name: private-llm-pm-app
description: >-
  Umbrella chart for the KCP (Platform Mesh) side of the Private LLM provider.
  Installs the APIExport, ProviderMetadata, and ContentConfiguration in a
  provider workspace. Install with kubeconfig/kube-apiserver pointing at the
  target KCP workspace.
type: application
version: 2.8.2
appVersion: 2.8.2
dependencies:
  - name: private-llm-pm-integration
    version: 2.8.2
    repository: file://../private-llm-pm-integration
