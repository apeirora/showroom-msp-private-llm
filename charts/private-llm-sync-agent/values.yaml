syncAgentOperator:
  enabled: true
  apiExportName: llm.privatellms.msp
  apiExportEndpointSliceName: llm.privatellms.msp
  agentName: llm-agent
  kcpKubeconfig: pm-kubeconfig
  image:
    tag: v0.7.0
  extraFlags: []
  crds:
    enabled: false
  hostAliases:
    enabled: false
    # For local Platform Mesh setup (Kind cluster):
    # enabled: true
    # values:
    #   - ip: "10.96.x.x"  # Traefik ClusterIP — run: kubectl get svc traefik -o jsonpath='{.spec.clusterIP}'
    #     hostnames:
    #       - "root.kcp.localhost"
    #       - "kcp.localhost"

publishedResources:
  enabled: true
  # Delete the token Secret copy in KCP when its APITokenRequest is deleted.
  tokenSecretCleanup: false
  namespace: api-syncagent
  serviceAccountName: private-llm-sync-agent
