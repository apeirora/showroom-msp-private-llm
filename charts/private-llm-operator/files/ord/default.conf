server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location = /pm-content.json {
    default_type application/json;
    try_files $uri =404;
  }

  location = /.well-known/open-resource-discovery {
    types { }
    default_type "application/json;charset=UTF-8";
    add_header Access-Control-Allow-Origin "*" always;
    add_header Cache-Control "public, max-age=300" always;
    add_header X-Content-Type-Options "nosniff" always;
    try_files /ord/configuration.json =404;
  }

  location ^~ /ord/ {
    types { }
    default_type "application/json;charset=UTF-8";
    add_header Access-Control-Allow-Origin "*" always;
    add_header Cache-Control "public, max-age=300" always;
    add_header X-Content-Type-Options "nosniff" always;
    try_files $uri =404;
  }

  location ^~ /ui-extensions/ {
    add_header Cache-Control "public, max-age=300" always;
    add_header Content-Security-Policy "default-src 'self'; connect-src https: http:; style-src 'self'; script-src 'self'; frame-ancestors *" always;
    add_header X-Content-Type-Options "nosniff" always;
    try_files $uri $uri/ =404;
  }
}
