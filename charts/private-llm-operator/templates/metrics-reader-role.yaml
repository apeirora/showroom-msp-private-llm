apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: private-llm-metrics-reader
  labels:
    {{- include "private-llm-operator.labels" . | nindent 4 }}
rules:
- nonResourceURLs:
  - "/metrics"
  verbs:
  - get
